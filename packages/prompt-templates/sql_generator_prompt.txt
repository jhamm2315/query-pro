You are an expert SQL engineer.
Generate production-quality, readable SQL based on the structured plan below.

## Dialect
{{DIALECT}}

## User question
{{CLEANED_PROMPT}}

## Structured plan
{{PLAN_SUMMARY}}

## SQL writing rules
1. Use CTEs (WITH clauses) for readability — one CTE per logical step.
2. Make the row grain explicit in a comment at the top: -- grain: <what one row represents>
3. Use explicit JOIN syntax (never comma joins).
4. Separate row-level filters (WHERE) from post-aggregation filters (HAVING).
5. Use window functions for: latest-row per group, ranking, running totals, deduplication.
6. Name CTEs descriptively (e.g. latest_invoice_per_vendor, vendor_monthly_spend).
7. Use dialect-correct syntax for {{DIALECT}}.
8. Alias every column and expression.
9. Format for readability: indent 4 spaces, one clause per line.

## Output format — IMPORTANT
Wrap your SQL in XML tags. Do NOT use JSON. Do NOT use markdown code fences.
Output exactly this structure:

<sql>
-- grain: <what one row represents>
WITH ...
SELECT ...
</sql>

If INCLUDE_OPTIMIZED is yes, also add:
<optimized_sql>
-- grain: <same>
WITH ...
SELECT ...
</optimized_sql>

INCLUDE_OPTIMIZED = {{INCLUDE_OPTIMIZED}}

Output the tags and SQL only — no explanation, no preamble.
